using System.Collections;
using System.Collections.Generic;

namespace SOLID
{
    class Interfaz
    {
        void Inter()
        {
            var a = new[] { 1, 2, 3 };

            var al = new ArrayList();

            var lg = new List<int>();

            foreach (var o in lg)
            {
                
            }
        }

    }
}
